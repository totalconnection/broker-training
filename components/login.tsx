'use client';
import {useState} from 'react';
export default function Login(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);return <><p className="eyebrow">MEMBER ACCESS</p><h2>Welcome back.</h2><p>Sign in to continue your learning path.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const form=new FormData(e.currentTarget);try{const r=await fetch('/api/auth/sign-in/email',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});const data=await r.json();if(!r.ok)throw Error(data.message||'Sign-in is not configured yet.');window.location.href='/app'}catch(e){setError(e instanceof Error?e.message:'Unable to sign in')}finally{setBusy(false)}}}><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label><button className="button primary" disabled={busy}>{busy?'Signing in…':'Sign in →'}</button>{error&&<p role="alert" className="error">{error}</p>}</form></>}
