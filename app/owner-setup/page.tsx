'use client';
import {useEffect,useState} from 'react';
export default function OwnerSetup(){const [token,setToken]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[complete,setComplete]=useState(false);useEffect(()=>{setToken(new URLSearchParams(window.location.hash.slice(1)).get('token')||'');history.replaceState(null,'',window.location.pathname)},[]);return <main className="setup"><h1>Set up your Freight Skills admin account</h1>{complete?<><p>Your admin account is ready. Sign in with the password you just created.</p><a className="button" href="/login">Go to sign in</a></>:<><p>This private invitation creates your owner account. Choose a password with at least 12 characters.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const form=new FormData(e.currentTarget);const password=String(form.get('password'));if(password!==form.get('confirm')){setError('Passwords do not match.');setBusy(false);return}try{const r=await fetch('/api/owner-setup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const result=await r.json();if(!r.ok)throw Error(result.error);setToken('');setComplete(true)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}><label>New password<input name="password" type="password" autoComplete="new-password" minLength={12} maxLength={128} required/></label><label>Confirm password<input name="confirm" type="password" autoComplete="new-password" minLength={12} maxLength={128} required/></label><button className="button" disabled={busy||!token}>{busy?'Creating account…':'Create my admin account'}</button>{!token&&<p>Open your private invitation link to activate this account.</p>}{error&&<p role="alert">{error}</p>}</form></>}</main>}
