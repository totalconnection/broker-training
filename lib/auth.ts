import {betterAuth} from 'better-auth';
import {pool} from './db';
async function sendEmail(to:string,subject:string,url:string){if(!process.env.RESEND_API_KEY||!process.env.EMAIL_FROM)throw Error('Email delivery is not configured');const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.EMAIL_FROM,to:[to],subject,text:`${subject}\n\n${url}\n\nIf you did not request this, ignore this email.`})});if(!r.ok)throw Error('Email could not be delivered')}
let cached:ReturnType<typeof createAuth>|undefined;
export function getAuth(){return cached??=createAuth()}
function createAuth(){if(!pool||!process.env.BETTER_AUTH_SECRET||!process.env.BETTER_AUTH_URL)throw Error('Authentication is not configured');return betterAuth({database:pool??undefined,baseURL:process.env.BETTER_AUTH_URL,secret:process.env.BETTER_AUTH_SECRET,trustedOrigins:process.env.APP_URL?[process.env.APP_URL]:[],emailAndPassword:{enabled:true,minPasswordLength:12,requireEmailVerification:true,sendResetPassword:async({user,url})=>sendEmail(user.email,'Reset your Freight Skills password',url)},emailVerification:{sendOnSignUp:true,sendVerificationEmail:async({user,url})=>sendEmail(user.email,'Verify your Freight Skills account',url)},rateLimit:{enabled:true,storage:'database',window:60,max:20},session:{expiresIn:60*60*24*7}});}
